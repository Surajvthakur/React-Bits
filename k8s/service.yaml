apiVersion: v1
kind: Service
metadata:
  name: react-bits-service
spec:
  type: NodePort
  selector:
    app: react-bits
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
      nodePort: 30007
